==== Expiration Date Pre-Ingest Plugin

The Expiration Date plugin adds or updates expiration dates which can be used later for archiving old data.

===== Installing the Expiration Date Pre-Ingest Plugin

The Expiration Date Pre-Ingest Plugin is not installed by default with a standard installation. The `catalog-plugin-expirationdate` feature can be installed manually in the ${ddf-catalog} application.

===== Configuring the Expiration Date Pre-Ingest Plugin

To configure the Expiration Date Pre-Ingest Plugin:

. Navigate to the *${admin-console}*.
. Select the *${ddf-catalog}* application.
. Select the *Configuration* tab.
. Select the *Expiration Date Pre-Ingest Plugin*.

The Expiration Date Pre-Ingest Plugin has these configurable properties:

include::{adoc-include}/_tables/org.codice.ddf.catalog.plugin.expiration.ExpirationDatePlugin-table-contents.adoc[]
